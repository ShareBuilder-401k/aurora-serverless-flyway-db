CREATE USER sample_application WITH LOGIN PASSWORD '${SAMPLE_APPLICATION_PASSWORD}';
GRANT CONNECT ON DATABASE ${DATABASE_PREFIX}_${ENV}_${REGION} TO sample_application;

REVOKE ALL PRIVILEGES ON DATABASE ${DATABASE_PREFIX}_${ENV}_${REGION} FROM sample_application;
REVOKE ALL PRIVILEGES ON ALL TABLES IN SCHEMA public FROM sample_application;
REVOKE ALL PRIVILEGES ON ALL FUNCTIONS IN SCHEMA public FROM sample_application;
REVOKE ALL PRIVILEGES ON ALL SEQUENCES IN SCHEMA public FROM sample_application;
GRANT USAGE ON SCHEMA public TO sample_application;
